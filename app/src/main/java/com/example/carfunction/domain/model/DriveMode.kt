package com.example.carfunction.domain.model

/** Available drive select modes. */
enum class DriveMode(val label: String) {
    COMFORT("comfort"),
    BALANCED("balanced"),
    SPORT("sport"),
    DYNAMIC("dynamic"),
}
