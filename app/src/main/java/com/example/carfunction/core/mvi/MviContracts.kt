package com.example.carfunction.core.mvi

/** Marker interface for all user intents (actions). */
interface MviIntent

/** Marker interface for all UI states. */
interface MviState

/** Marker interface for all one-shot side-effects. */
interface MviEffect
